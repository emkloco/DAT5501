# Calendar Printer

## Overview
A simple Python utility script that generates and prints a formatted calendar for a specific month and year.

## Usage
Run the script from the terminal:
```bash
python calendar_printer.py